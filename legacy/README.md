# Legacy material

This directory contains the original identification, baseline-generation,
plotting scripts, and saved figure artifacts. They are retained for provenance
but are not part of the tested reproduction workflow.

The active workflow is under `scripts/`, reusable MATLAB code is under `src/`,
and published result data is under `data/published/`. Figure 4 remains outside
the active reproduction scope.
